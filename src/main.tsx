import React from "react";
import ReactDOM from "react-dom/client";
import { TaSklistApp } from "./TaSklist";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <TaSklistApp />
  </React.StrictMode>
);
